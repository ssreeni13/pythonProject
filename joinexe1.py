#String Join (str.join(iterable)): Combining elements of an iterable into a single string.

words = ["Python", "is", "fun"]
combined = " ".join(words)  # Combines the words with spaces
print(combined)
